# Runtime dependencies. For development (pytest, ruff) install the package
# with the dev extra instead:  pip install -e ".[dev]"

# Core data + stats
numpy>=1.26,<3.0
pandas>=2.1,<3.0
scipy>=1.11,<2.0
scikit-learn>=1.3,<2.0

# Love-plot of pre/post covariate balance
matplotlib>=3.7,<4.0

# LLM closing summary (Anthropic Claude)
anthropic>=0.40,<1.0

# CLI + env
python-dotenv>=1.0,<2.0
